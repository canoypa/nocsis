name: Deploy to Staging

on:
  pull_request_target:
  workflow_dispatch:

concurrency:
  group: Deploy to Staging

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      env: Preview
    secrets: inherit

{
  "functions": {
    "source": "functions",
    "predeploy": [
      "pnpm --prefix \"$RESOURCE_DIR\" run lint",
      "pnpm --prefix \"$RESOURCE_DIR\" run build"
    ],
    "runtime": "nodejs20"
  }
}

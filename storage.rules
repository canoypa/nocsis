rules_version = '2';

// クライアントから Cloud Storage へ直接アクセスする経路は現状存在しない。
// 全面拒否し、必要になった時点でパス単位で緩和する。
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
